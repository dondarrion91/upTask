<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Reestablecer password
        p Hola , has solicitado reestablecer tu pasword has click en el siguiente enlace , este enlace es temporal , si se vence vuelve a solicitarlo
        a(href=`${resetUrl}`) Ir a Resetear el Password
        p si no puedes acceder a este enlace , visita #{resetUrl}